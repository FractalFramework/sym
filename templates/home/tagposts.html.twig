{% extends 'base.html.twig' %}

{% block title %}Posts contenant le tag {{ tag }}{% endblock %}

{% block body %} 

<div class="example-wrapper">
    <h1 class="display-5">Posts contenant le tag {{ tag }}</h1>
    <table class="table table-hover">
        <tr class="table-info">
            <th>Image</td>
            <th>titre</td>
            <th>auteur</td>
            <th>Dernière modification</td>
            <th>Voir</td>
        </tr>
    {% for post in posts %}
        <tr class="table-light">
            <td><img src="{{ asset(post.image) }}" width="220px" /></td>
            <td>{{post.username}}</td>
            <td>{{post.title | raw}}</td>
            <td>{{post.UpdatedAt | date("d/m/Y")}}</td>
            <td>
            <a class="btn btn-primary" href="{{ path("show_post", {"slug": post.slug }) }}">Afficher</a>
            </td>
        </tr>
    {% endfor %}
    </table>
</div>
{% endblock %}
